# Ignite：从首次接管到持续交付的 AI 执行审计清单

审计日期：2026-09-14；整改复核：2026-09-18。原始审计基线：`28b9b5972e968dffa54d560de4631e1486214fe6`；第一阶段整改提交：`323ee8dd246b360cd8abab5468b028ca8c67a58c`。下文标明为“审计基线”的内容保留整改前判断，不代表当前状态。

第二轮复核：补充架构、项目管理、已有数据升级、环境适配与合并交付问题；保留原 E01—E16 编号，新增项接续编号。对“报告是否完整”的判断是：第一轮覆盖了主要使用环节，但缺少跨环节的职责设计与演进验证，本轮补齐这些层面。

阅读建议：先读结论，再看第五节处理顺序和第八节最小执行架构；需要实施时再查第三节的 21 组问题、证据和验收条件。已复现、源码判断与尚待演练分别标识，不声称已穷尽所有缺陷。

本报告回答：**用户已经说清楚目标后，AI 能否自行接管项目，把修改、验证、恢复和交付做好，让用户不用记住工程细节、催促测试或判断真假完成？**

边界是执行工程，不讨论产品方向、商业模式或另做一套视觉设计。涉及外观时，只审计“既定外观要求如何被落实、哪些模板内容可以替换”。下文“审计方法”“问题复现”和原始方框保留整改前的基线事实；2026-09-17 起的实施进度以下表为准。没有重新打开或改写已完成的 IGT-002。

## 整改进度（IGT-003）

“已落地”只表示对应代码、规范和定向测试已存在；“部分落地”表示还有原验收场景未跑通或尚无法给出强保证。全量接管成功率和“100 分”必须由真实多轮使用数据验证，不能由本表主观宣布。

| 项  | 当前状态 | 已落实的结果与未闭环处                                                                                                              |
| --- | -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| E01 | 部分落地 | 安装前预检给出下一步；独立干净克隆从无依赖到安装、数据库、构建及 6 条 E2E 已通过，浏览器缓存和各 AI 工具入口仍待实测。              |
| E02 | 部分落地 | 结构化项目身份和模板远端拦截已建立；完整 clone、Use template、新 Git 历史仍需真实采用演练。                                         |
| E03 | 部分落地 | Site 配置用 TS AST、环境值按引号解析，身份不再靠产品正文措辞；模板诊断与服务端共用环境值规则，动态计算 Site 配置仍未覆盖。          |
| E04 | 部分落地 | 采用规范有资产与检查映射；尚未实际更换整个外壳或删除 Tasks。                                                                        |
| E05 | 部分落地 | 稳定目标、约束、授权、交付对象进入 Plan 指纹，REQ 必须映射目标；仍需独立核对原始用户目标没有被删减。                                |
| E06 | 已落地   | 安全文案/资产使用同一风险分类，CI 文档与格式检查保留，不强制伪造业务 Plan；规则和行为文件仍需 Plan。                                |
| E07 | 已落地   | 不完整未来 draft 不挡当前任务、晋级会被拦；`create:change` 提供存量修改草稿入口。                                                   |
| E08 | 部分落地 | 精确用例名、`.test.tsx` 发现和空验收回调已拦截；断言语义和测试被故意放宽仍不能由标签自动证明。                                      |
| E09 | 部分落地 | 新证据核对本地运行记录与日志，CI 对照 Plan 用例；拥有全部写权限的执行者仍需独立 CI 重跑形成更强信任。                               |
| E10 | 部分落地 | `ignite next` 可派生当前目标、约束、run 和下一步；跨会话完整接续旅程未演练。                                                        |
| E11 | 部分落地 | 总时限、当前步骤、最后输出、精确取消和坏记录隔离已实现；真实安静子进程取消已通过，父进程崩溃/孤儿恢复仍待演练。                     |
| E12 | 部分落地 | 发布检查复用同输入集成证据，CI E2E 使用 build job 产物；模块改动选择现有对应测试、未知模块回退全量，冷/热耗时和大项目效果尚未验证。 |
| E13 | 部分落地 | 全零 before 用根提交作显式基线并有 fixture 测试；远端首推、PR、浅历史仍待实测。                                                     |
| E14 | 已落地   | 本次独立 Plan 可合并，Release 整体状态继续按全部 Plan 推导，并有同 Release 未来 draft 的回归测试。                                  |
| E15 | 部分落地 | Task API 路由、Zod 请求字段与实际响应对照 OpenAPI；其它接口语义及视觉/正文事实不具备通用强校验。                                    |
| E16 | 部分落地 | 接续摘要不把本地 done 冒充推送或部署；真实远端和可访问入口仍未自动核实。                                                            |
| E17 | 部分落地 | 逐个 migration 边界注入样本并检测数据丢失、已发布 SQL 被重写；当前仓库仅有初始 migration，真实多版本升级尚无实例。                  |
| E18 | 部分落地 | SQLite 隔离适配入口已集中，换 provider 时明确拒绝旧验证；尚未选择第二个 provider，不能宣称替换能力已实测。                          |
| E19 | 部分落地 | 浏览器选择及公开构建变量进入指纹，测试环境按允许列表组装；未来 provider 变量仍须随能力登记。                                        |
| E20 | 部分落地 | 依赖环、状态晋级、提交归属和冲突概率更低的 ID 有测试；真实并行 worktree 合并仍未演练。                                              |
| E21 | 部分落地 | Squash 后可重新绑定最终提交并强制重验；rebase 与全新克隆中的全过程尚未实测。                                                        |

第一阶段提交 `323ee8d` 已通过 [集成检查](docs/others/evidence/runs/run-20260917150313-9d7669.json) 与 [发布检查](docs/others/evidence/runs/run-20260917151513-3edd2f.json)。第二阶段提交 `0634cff` 已通过 [集成检查](docs/others/evidence/runs/run-20260917173404-72fc80.json) 与 [发布检查](docs/others/evidence/runs/run-20260917174243-f77d04.json)：包含 Vitest 全量 94 项、类型检查、lint、格式与文档检查、治理校验、迁移冒烟、生产构建，以及桌面和移动 Chromium 共 6 条生产构建 E2E。每份证据只对应其被测版本；新增工作区改动仍需重验。独立 CI 尚未核实，21 项也未全部满足原验收场景。

## 审计基线结论（整改前）

Ignite 已有较扎实的执行基础：职责分层清楚，规格和测试可追踪，检查有输入指纹、运行记录和提交绑定，发布状态可推导，桌面与移动视口有真实浏览器回归。下一轮开发可以沿用这些基础。

但当前最强的是“检查已经描述好的任务”，从“接收目标”到“自动组织完整执行”的衔接仍依赖 AI 临场理解。首次采用、可变资产识别、失败诊断、跨会话接续和最终交付还没有形成统一的、可直接消费的下一步指引。

还有一个重要准确性边界：**证据格式一致，不等于证据来源可信；测试通过，不等于用户目标完整实现。** 本次在隔离样例中成功构造了没有执行测试、却能通过 `set-status done` 的证据。因此，之前“伪造证据都不能完成 Plan”的表述过强，应修正为“能够拒绝多类缺失、过期和错配证据，但没有独立证明执行来源”。此前真实跑过的 67 项测试和 E2E 仍然是有效观察，并未因此变成没有运行。

以“用户不用维护执行准确性”为标准，现在还不足以有依据地宣布 9.5 分。应把高分转换为后文的可重复采用与开发演练结果，而不是继续增加规范条数。

从架构上，下一步最值得投资的是把“目标契约、当前状态、允许的下一步、执行结果和交付结果”接起来。现有 CLI 主要负责校验、运行检查和写状态；源码实现仍由宿主 AI 完成。需要把两者之间的执行协议做清楚，让常规调度、诊断和接续由系统提供确定依据。具体责任分工见第八节。

## 审计方法与证据范围

遵循用户给出的六项思维习惯：先查实际文件和输出；定位影响交付的关键环节；寻找反例；用隔离样例验证；补齐前后依赖；最后为每个问题定义可验收的处理结果。

- **[复现]**：本次运行过最小样例并观察到结果。
- **[源码]**：由当前实现或规范直接支持，尚未演练全部用户场景。
- **[待演练]**：由结构推出的风险，不声称已经发生。
- **P1**：优先修正，关系到错误完成、执行卡死或首次采用失败。
- **P2**：随后修正，主要影响效率、上下文和持续修改体验。
- `[ ]` 表示尚未处理；`[x]` 只用于已确认存在的能力。

主要检查了 AGENTS、工具桥接、README、采用规范、工作流、Feature/Plan 模板、CLI、状态与证据校验、运行锁、风险选择、脚手架、运行时诊断、CI、测试结果检查器，以及品牌、导航、API 地址、主题和 E2E 的关联。

本次实际运行了 `pnpm runtime:check`、`pnpm ignite status --json` 和隔离审计探针。运行时通过；审计开始时状态报告没有结构错误。没有重新运行全量测试，也没有调用外部 AI 工具完成冷启动实测。

2026-09-17 收尾时重新检查运行时和只读状态，结果仍为运行时通过、`failures: []`。这说明当前结构校验可通过，不代表本报告中的执行反例已经修复。

已有基线证据为 [集成运行](docs/others/evidence/runs/run-20260913124330-98c3b2.json) 和 [发布运行](docs/others/evidence/runs/run-20260913165417-21b1ef.json)，被测实现提交为 `552f0aa`。上一轮另已完成开发模式 E2E 和独立干净克隆复验。它们证明相应版本的已测路径，不代表覆盖本报告新增的所有反例。

工程判断采用两个具体原则：符合规格的验证，与在预期使用环境中满足用户需要的确认，应分别提供证据；这一划分见 [NASA 产品实现手册](https://www.nasa.gov/reference/5-0-product-realization/)。失败处理应通过现象、假设和验证收敛原因，不能把重复运行当成诊断，见 [Google SRE 故障排查方法](https://sre.google/sre-book/effective-troubleshooting/)。以下建议是对本项目的应用判断，不代表通过这些机构的标准认证。

## 一、沿实际执行顺序推演

这里列的是 AI 执行时必须得到的判断与可观察动作，不推测某个模型的私有思考过程。

| 环节             | AI 应当先得到什么                  | 当前路径                      | 缺口与影响                                                   |
| ---------------- | ---------------------------------- | ----------------------------- | ------------------------------------------------------------ |
| 1. 初次进入      | 规则入口、运行环境、可用工具       | AGENTS、桥接、runtime:check   | 缺少安装前的环境与工具能力预检；桥接存在不等于工具已读取     |
| 2. 判断身份      | 维护模板，还是基于模板开发新项目   | template-baseline / adopted   | 完整克隆仍保留模板 origin，身份和推送目标未联合校验          |
| 3. 承接目标      | 用户目标、已授权范围、交付对象     | Feature、Plan、open_questions | 原始目标与验收项之间没有完整性对照，授权信息主要靠正文       |
| 4. 识别可改内容  | 哪些是示例，哪些是需保留的行为约束 | adoption 的资产分级           | 名称、路由、地址、样式与测试的关联不集中，容易漏改或过度保留 |
| 5. 准备项目      | 独立身份、secret、环境、历史       | 手动步骤与 adopt-history      | 仍有人工走查要求；采用诊断受格式影响                         |
| 6. 准备任务      | 合法 Plan、依赖、最小相关上下文    | JSON 元数据、脚手架           | 简单改动流程偏重；未来草稿和全局门禁相互干扰                 |
| 7. 修改实现      | 当前规格、真实代码、影响关系       | 模块、Hook、RPC、Prisma       | 主链路清楚；测试选择和模板示例耦合仍需 AI 人工识别           |
| 8. 运行目标测试  | 真实行为断言与失败原因             | Vitest、Playwright、AC 检查器 | 标签追踪强于语义覆盖；用例选择后缀不生效                     |
| 9. 处理失败/中断 | 已完成什么、失败原因、唯一下一步   | run_id、日志、心跳、锁        | 活着但无进展的进程缺少处理闭环；没有完整任务接续包           |
| 10. 集成验证     | 最低充分检查和可复用结果           | integration → release         | 大量验证重复，正文契约变化又可能不使证据失效                 |
| 11. 回写与交付   | 当前事实、实际成果入口、可信证据   | Design、done、Release、CI     | 证据来源未独立验证，done 没有统一表示用户如何拿到成果        |
| 12. 下一轮开发   | 当前状态和与本次有关的历史         | status、文档、历史 Plan       | 历史可追踪，但检索和恢复上下文仍需 AI 自行拼装               |

顺畅体验应是：用户提供已确定的目标，AI 自动形成执行输入；工具检查缺什么；能自行解决的继续解决；按范围实现与验证；失败进入诊断；证据充分后给出成果入口。只有目标出现新的实质歧义、凭据缺失或必须由用户作出的授权选择时，才集中反馈一个具体阻塞。

## 二、应保留的能力

- [x] **模块化单体与纵向切片。** `src/app`、`src/modules`、`src/server` 分工清楚；Hook → RPC → 路由 → Prisma 给 AI 提供稳定的修改路径。
- [x] **规则入口集中。** AGENTS 是共同入口，工具桥接不各自维护一份长规则。应保留这一方向。
- [x] **要求、验收和测试可以关联。** REQ、AC、Plan、测试标签帮助定位遗漏；不应退回纯自然语言宣布完成。
- [x] **实际差异决定检查下限。** Plan 基线与 Git 差异覆盖已提交、暂存和工作区变化，避免仅看最后几个编辑文件。
- [x] **实际测试结果门禁。** 当前检查器拒绝带 AC 标签的跳过、预期失败和重试后通过等情况，明显强于只搜索标签。
- [x] **验证工作区互斥与运行查询。** 已有原子创建锁、运行标识、心跳和孤儿状态；同工作区重复构建得到控制。
- [x] **历史与当前证据分开。** 旧 Plan 按被测快照校验，新任务按当前输入校验，支持持续演进。
- [x] **测试环境隔离。** E2E 使用临时数据库和可用端口，桌面/移动视口均有覆盖；认证基线不依赖真实邮件。
- [x] **没有为简单业务预设过多空层。** 规范允许简单 CRUD 直接访问 Prisma，复杂性出现后再增加业务服务层。

## 三、按链路排序的整改清单

### E01 · P1 · 首次进入缺少完整的自动接管步骤

- [ ] **完成冷启动接管。** [源码] README 的开始步骤假设已有 nvm/Corepack；AGENTS 首步要求 `runtime:check`，但依赖标记要安装后才存在。采用规范要求 E2E，却没有在相同路径中包含浏览器安装预检；CI 则单独安装 Chromium。工具桥接校验仅检查文件含 `AGENTS.md`，没有证明实际加载；`.ai/skills` 当前也没有项目专属执行技能。

**影响：** 熟悉工程的 AI 能自己补齐步骤，陌生工具或新机器上的 AI 可能反复安装、调用错误环境，或要求用户逐项处理。

**处理：** 在已有 CLI 中增加安装前可运行的预检与接管结果，报告 Git、Node/pnpm、依赖平台、浏览器、网络、数据库准备状况；缺失项返回明确动作。为声明支持的工具做入口冒烟验证，记录能力要求，而非声称任意 AI 都自动兼容。删除采用规范中例行“再手动完成一次”的强制要求，用自动浏览器旅程覆盖。

**验收：** 无依赖、无浏览器缓存的干净环境中，从目标到本地运行由 AI 完成；已有 `.env` 不被覆盖。工具缺权限时只报告相应阻塞；不让用户执行测试或找日志。

依据：[README](README.md)、[采用规范](docs/standards/adoption.md)、`runtime-doctor.mjs`、`validateAgentBridges()`、CI 浏览器安装步骤。

### E02 · P1 · 克隆模板后的 Git 归属没有被接管

- [ ] **将项目身份与交付目的地一起检查。** [源码] 当前 origin 的 fetch/push 都是 `git@github.com:morewhyhan/Ignite.git`。`adopt-history` 处理新 Git 历史中的旧 Plan，但不检查完整克隆的 remote；采用清单也未建立明确的推送目标记录。

**影响：** 更名、换 UI 后，AI 仍可能把新产品推回模板。普通 clone 本身不会修改模板，风险发生在后续 push。

**处理：** 接管时区分“维护模板”和“创建衍生项目”，记录源仓库、项目仓库及交付目标。用户已给出目的地就使用；缺失时仍可完成本地实现和验证，交付阶段再明确缺少目的地。检测衍生身份与模板 push URL 冲突，不删除用户 Git 历史来回避问题。

**验收：** 完整 clone、Use template、新仓库初始化三种路径各有演练；衍生项目无法在未明确选择维护模板时自动推回源仓库。

### E03 · P1 · 采用诊断把语法格式当成语义

- [ ] **让采用检查读取实际配置值。** [复现] 合法的双引号 `siteConfig` 被报告无法读取；已采用项目把默认 secret 写成单引号时，doctor 返回成功。状态识别也依赖中文正文中的特定字符串。

**影响：** AI 合理改写配置可能被迫追查“为什么红了”；相反，真正应该更换的配置又可能遗漏。生产环境还有独立校验，本项复现不代表生产校验被绕过。

**处理：** 项目身份使用可解析的数据源；TS 配置使用 AST 或受控数据导出，环境文件按实际解析语义读取。CLI 和服务端共用必要的值校验，避免两份正则产生分歧。

**验收：** 单/双引号、空格、环境注入等合法写法得到一致结果；默认 secret 不论语法形式都被正确识别；不输出 secret 本身。

依据：`scripts/template-doctor.mjs`、`templateMode()`、本次隔离探针。

### E04 · P2 · 可改资产已有分级，但缺少关联清单

- [ ] **把品牌、地址、路由、样式和示例的修改入口串起来。** [源码] `adoption.md` 已明确产品外壳可换，Tasks 可保留/改造/删除；`design.md` 也允许更新视觉基线。但 `docs/README.md` 强调无明确要求时不得删除/重命名，当前设计描述了固定粉色风格，E2E 又直接使用首页按钮文案和 `/dashboard` 路由。缺少一次采用决定在这些位置之间的映射。

**影响：** AI 可能只改 `siteConfig`，留下技术栈介绍和模板说明；也可能因为保留基线的措辞，误把当前主题和路由当成永久限制。测试红了时，容易在“恢复模板 UI”与“更新测试”之间判断错误。

**处理：** 在现有采用规范中维护下表这样的资产表，并将本项目选择写入产品规格；无需再增加一套平行文档。行为不变量与展示内容分别列明。

| 资产       | 当前入口                                       | 执行时应采用的原则                                                           |
| ---------- | ---------------------------------------------- | ---------------------------------------------------------------------------- |
| 品牌与说明 | `src/config/site.ts`、package、README、Landing | 按给定产品身份替换；检查残留，不盲目全局替换所有 ignite 字符串               |
| 服务地址   | `.env` 的 APP_URL、`src/lib/api-client.ts`     | 浏览器 RPC 已使用当前 origin；服务端 origin 按环境配置，不能写入临时预览地址 |
| 仓库链接   | Git remote、README 的项目链接                  | 与本项目归属一致；官方技术文档链接无需改为产品地址                           |
| 路由与导航 | `src/app`、navigation、登录跳转、E2E           | 页面结构可按已定目标调整，同步入口、重定向和测试                             |
| 样式与主题 | globals.css、theme 模块、共享 UI、design.md    | 当前配色是基线事实；既定新风格可替换，保留所需交互与可访问性                 |
| Tasks 示例 | Schema、路由、Hook、页面、规格、测试           | 有明确去留决定，删除后仍保留认证和权限不变量的独立测试                       |
| 工程边界   | Hook/RPC、服务端 secret、权限、迁移            | 默认保留；需要替换时按有依据的存量变更处理                                   |

**验收：** 按已定目标换品牌、首页、主路由和主题后，模板残留被识别，入口可用，测试断言对应新产品；无需用户逐文件指认。删除 Tasks 后实际跑完整验证，而不只运行“没有访问 Tasks 页面”的认证用例。

### E05 · P1 · 用户目标与执行契约还没有完整绑定

- [ ] **锁定已知目标、约束和授权，防止执行时漂移。** [复现＋源码] Plan 有 outcome/requirements/acceptance 等结构化字段，但非目标、保留事项、授权说明、假设和回滚要求主要在正文。`stablePlanContract()` 不包含它们，Plan 正文又被排除出输入指纹。探针新增“不得删除现有用户数据”的约束后，指纹没有变化。

**影响：** 会话恢复可能漏掉已确定约束；修改验收相关正文后，旧证据仍可被视作同一输入。`open_questions=[]` 只能证明数组为空，无法证明用户目标都已承接。

**处理：** 将影响结果的目标、非目标、必须保留内容、接受的假设、交付对象和执行授权放入稳定契约，或对正文中明确的契约区计算摘要；过程日志仍不参与指纹。保留用户原始目标引用，生成“目标条目 → REQ → AC”的缺项检查。已授权内容应持续有效，常规实现选择无需反复确认。

**验收：** 修改必须保留的行为会使相关旧证据失效；只更新进度不会失效。新会话能找回所有约束。删除一个用户明确目标时，系统提示范围变更而不是悄悄完成。

### E06 · P2 · 轻量修改与统一流程存在冲突

- [ ] **为低影响任务提供低成本的合法路径。** [复现＋源码] 工作流允许纯错字不建 Plan，但统一 check 必须有 Plan，CI 的差异覆盖又要求完成 Plan。风险规则只将少数 README/静态资源视为 dev；纯文字根目录报告 `EXECUTION_AUDIT.md` 会被判 integration。非 docs Plan 一律要求 integration 和 release。

**影响：** 很小的修改也容易被迫编写无业务意义的 REQ/AC、提交实现，再跑全量发布测试；AI 会在遵守规范和减少无效工作之间摇摆。

**处理：** 明确轻量文档修改的统一准入条件、范围校验和证据种类。正文规范、CLI 与 CI 用同一个分类函数。规则、Feature、测试和执行配置仍按实际风险检查；不能把所有 Markdown 一概降低等级。

**验收：** 纯文案、真实行为变化、执行规则修改分别选择适当检查。当前这类只交付报告的任务可以合法交付，不需伪造行为验收测试。

### E07 · P2 · 未完成草稿会影响无关任务，任务创建偏重新模块

- [ ] **分开草稿结构检查与实施准入检查。** [复现] 未完成的未来 draft 引用尚不存在的 REQ/测试，会进入 `validateAllPlans()` 的错误列表；集成检查调用全局 validate，因此受影响。未改动但已完整合法的草稿可以存在，不能把两种情况混为一谈。

**影响：** 人想先记下下一项工作，也要提前把验收和测试布置好；AI 可能为让当前检查通过而处理无关需求。`create:module` 可创建新模块，却没有同等方便的存量修改 Plan 入口，后者需手填模板。

**处理：** draft 允许可识别的待补项，ready/active 严格检查实施输入；无关草稿问题显示为待办，结构损坏另行报错。复用现有元数据模型增加存量 Plan 创建入口，并给出合法的下一条命令。

**验收：** 在一个已完成任务旁放置未展开的未来需求，不阻塞当前交付；该 draft 自己转 active 时会被缺项检查挡住。依赖图与状态切换还需满足 E20 的一致性要求。

### E08 · P1 · 测试追踪不能独立判断目标是否实现

- [ ] **加强验收断言与目标之间的独立核对。** [源码] 静态分析确认 AC 标签，运行检查器确认 tagged test 通过；但空断言、错误期望、把实现错误同步写成期望值，仍可能通过。当前脚手架的红测试使用 `expect.fail`，它提醒实现尚缺失，但还不是业务失败证据。

**处理：** 将已明确的验收目标转成具体输入/输出、权限和失败例子，测试审查以这些例子为依据。要求关键新行为在实现前出现可解释的失败，测试被放宽时记录目标依据；高风险逻辑加入有针对性的故障注入或变异检查。可由 AI 自动做第二轮独立审查，无需把测试审查交回用户；但同一模型重复阅读也不等于统计独立证明。

**验收：** 故意移除所有权过滤、持久化或错误处理时，对应用例必然失败。把业务测试替换成空函数，不能仅凭 AC 标签获得目标已满足的判定。

- [ ] **修正用例选择后缀的含义。** [复现] Plan 模板支持 `file::用例名`，但校验与结果匹配均截取文件路径。指定 exact behavior 后，只提供同一 AC 的另一条 passing 结果，结果检查器仍返回无错误。应真正匹配后缀，或取消该语法并明确只支持文件＋AC 粒度。

**验收：** 计划指定的用例没有执行时，不会被同文件的另一条用例顶替。

- [ ] **统一计划声明、静态扫描和测试执行器的发现范围。** [源码] `state.mjs` 接受 `.test.tsx`，`checks.mjs` 也会选择它，但 `vitest.config.ts` 的 include 只有 `.test.ts`。静态追踪还扫描整个 `tests/` 下的 TS/TSX，范围大于实际执行目录。于是“找到了 AC”与“执行器会运行该测试”可能分离。

**处理与验收：** 使用执行器实际发现的用例清单，核对 Plan 需要的测试均可发现、均已执行；`.test.ts`、`.test.tsx`、辅助文件、排除目录和过滤参数分别有明确结果。规范入口能因缺跑而失败是已有保护，但应在进入昂贵检查前报出准确原因。

### E09 · P1 · 缺少独立的执行证据来源

- [ ] **让系统能够区分执行结果与 AI 写出的结果说明。** [复现] 探针在临时 Git 仓库中，使用当前指纹、真实 commit、预期命令列表、任意合法 64 位日志摘要，生成两份 passed JSON；没有执行其中的命令，`setPlanStatus('IGT-900', 'done')` 仍成功。`validatePlan()` 检查摘要的格式，不检查日志内容来源，也没有不可由实现侧伪造的执行凭据。

**影响：** 对通常的遗漏、过期引用有防护，但 AI 错把“补写证据文件”当成修复方式时，系统不能独立阻止错误完成。这与用户希望不用人工维护准确性的目标直接相关。

**处理：** 先明确可信边界。由实际 runner 输出规范化测试结果和命令结果，finalize 从原始运行通道读取，核对日志摘要与测试清单；缺失时触发重跑。最终验证应有实现侧之外的执行来源，例如受控 CI 在指定提交重跑并产出结果。仓库内增加一个同样可被 AI 改写的签名文件并不能解决信任问题。对同时具有规则、校验器、日志和密钥任意写权限的执行者，不能承诺绝对防伪。

**验收：** 本次“仅构造 JSON”反例失败；真实执行可以完成；没有本地日志的克隆可通过独立重跑重新建立证据。用户不需要手读日志判断真实性。

依据：`validatePlan()`、`durableManifest()`、`publishEvidence()`。现有 `ignite-cli.test.ts` 的合成 receipt 是合法的结构测试技术，但不能据此推导已覆盖真实来源验证。

**CI 补充边界：** [源码] CI 直接调用 `pnpm test` 和 `pnpm test:e2e:production`，没有传入当前 Plan 验收集合；`acceptanceFailures()` 缺少 `IGNITE_PLAN_ID` 时只检查实际发现的 tagged test。CI 有真实重跑价值，但没有独立核对“计划要求的所有用例都执行了”。应由本次差异派生待交付 Plan 的验收集合，在 CI 中和执行器结果对账，不能把任意本地 receipt 当成这个集合已完成的证明。

### E10 · P2 · AI 缺少小而完整的接续上下文

- [ ] **生成当前任务的接续摘要与下一步。** [源码] `status --json` 返回全量 Plan 元数据、历史和 Release；`run status` 摘要有状态和时间，但不包含失败命令、可直接执行的恢复步骤、当前子任务或已验证/未验证差异。完整信息分散在 Plan、日志与文档中。

**影响：** 人不必再亲自记状态，但 AI 仍需反复读历史，用户经常要说“继续”“现在做到哪了”。长期积累后，检索成本会增加。

**处理：** 从现有真源派生一个任务摘要：目标/约束、工作区与基线、当前子任务、最新 run、失败原因、接下来允许的动作、必要文档路径、真正需要用户提供的事项。默认只展示当前相关内容，历史按需查询；不建立第二份手写状态台账。

**验收：** 新会话只读入口与任务摘要就能安全接续，知道应该等待、诊断、修改还是交付；不重复询问已回答的问题，不重复启动运行。

### E11 · P1 · 运行存活与任务进展还没有分开

- [ ] **补齐超时、无进展、取消与故障诊断。** [源码＋待演练] `derivedRunStatus()` 在 runner 或直接子进程仍存活时返回 running；`runChild()` 没有命令总时限和取消入口。心跳说明控制进程还在工作，不证明子命令有进展。父进程/直接子进程信息也不足以覆盖所有孙进程场景。当前真实锁竞争用例覆盖抢锁，不等于覆盖全部进程崩溃与恢复。

**影响：** 挂起安装或构建可能长期占用工作区；换一个 AI 仍只得到 running，需要用户判断是否该停。

**处理：** 区分控制进程存活、子命令存活、最后输出、总耗时和阶段进展。设可配置的阶段预算，提供安全取消与诊断；仅无输出不自动判失败。管理本次运行创建的进程组，不按不可靠 PID 杀别人的进程。损坏的单条本地 run JSON 应隔离报告，避免整张运行表无法读取。

**验收：** 演练长时间安静但正常的命令、真正挂起、父进程中断、残留子进程和损坏记录；正常任务不重复跑，异常任务有可执行恢复动作。把实际崩溃演练与模拟状态测试分别记录。

**项目管理补充：** `workflow.md` 已规定相同失败连续两次且没有新证据时停止机械重试；需要把这条文字规则落实到可消费的诊断与恢复结果中。重试预算应对应具体失败类别和诊断依据，记录已排除的原因。达到时间或重试预算时，系统应产出当前阻塞、已完成成果和恢复条件；不会因为预算到期就改为 done，也不会在没有新信息时无限重复。预算服务于减少无效消耗，不替代任务验收。

### E12 · P2 · 检查可靠，但重复成本偏高

- [ ] **让验证结果按被测输入和检查步骤复用。** [源码＋已有证据] integration 包含类型、lint、格式和测试；release 的 verify 再执行同类检查。两层 run 不同，当前复用不会消除这些重复。CI build 成功后，E2E job 又重新构建。发布证据中 verify 为 386201 ms，E2E 命令为 58581 ms；这是一轮环境的耗时，不是普遍性能基准。

**处理：** 保留集成门禁与完整发布门禁的意义，在相同提交、依赖、环境与测试范围下复用步骤结果或构建产物。变更时按依赖失效，未知影响仍全量检查。扩展模块不应不断给 `targetedTests()` 增加 Tasks 式硬编码；优先从 Plan 映射与模块关系获得范围。

**验收：** 同输入的 integration → release 不重复执行可安全复用的步骤；源代码、环境或构建参数改变会重新执行；省下的时间不会来自遗漏验收。记录冷/热运行耗时后再决定是否优化历史扫描与 Git 调用。

### E13 · P1 · 新仓库首推的 CI 基线边界未覆盖

- [ ] **处理不存在父提交的发布起点。** [复现＋源码] CI 使用 PR base 或 push before 作为 DIFF_BASE；对全零 before 的隔离输入，`validateCiCompletion()` 返回 `CI DIFF_BASE is not a real commit`。完整 GitHub 首推事件尚未在本次远端实测，但对应本地分支确定会拒绝该值。

**影响：** 模板第一次推到新仓库时，可能功能都对、自动检查却失败；用户又要理解 Git 事件来修复。

**处理：** 显式区分首推、普通 push、PR 和浅历史；首推使用明确的初始快照覆盖规则或空树比较，不能简单忽略全部变更。结合 E02 的新项目身份与历史采用建立完整启动流程。

**验收：** 以上四类 Git 基线都有可重复检查，首推既能通过正常采用结果，也能发现没有纳入验证的改动。普通开发中的 squash/rebase 合并边界另见 E21。

### E14 · P2 · Plan 独立交付与 Release 整体完成之间有耦合

- [ ] **区分本轮变更可合并与整个 Release 已完成。** [源码] `validateCiCompletion()` 要求本次涉及 Plan 所属的 Release 为 done。同一 Release 包含一个完成 Plan 和一个未来 draft 时，当前交付会被 Release 未完成阻止。

**影响：** 已约定一个 Plan 可独立验收、集成和回滚，但实现时可能被迫等完整 Release，或把每个小任务都拆成单独 Release 来绕开。

**处理：** 明确合并门禁检查本次交付的 Plan 与影响范围；发布门禁检查整个 Release 范围。若坚持 Release 是原子交付单位，应把这种限制写清楚，避免与独立 Plan 约定冲突。

**验收：** 完成一个独立任务不会因同 Release 的未来工作被迫扩展范围；真正发布整个 Release 时仍要求范围内任务齐备。

### E15 · P2 · 事实文档有结构校验，语义漂移仍可能遗漏

- [ ] **按事实类型补强漂移检查。** [源码] 数据库设计已对 migration 做结构对比；OpenAPI 的实现对比主要检查 method/path 集合，未完整比较请求、响应字段和错误语义；PlantUML 主要检查起止标记。视觉设计、路由文档和部分正文事实尚无等强度自动验证。

**影响：** 新 AI 可能准确读到一份过时的说明，随后按错误上下文修改。文档数量增加不自动意味着上下文更准确。

**处理：** 可从 Schema/契约确定性生成的内容尽量生成；由人或 AI 维护的解释保留为解释，并记录相关输入版本。请求/响应与错误契约增加有价值的行为对照；无需为每句说明写字符串测试。

**验收：** 仅改接口响应字段或权限行为、没有同步相应契约时可以检测；正常重命名、主题替换不因旧说明被迫回退。

### E16 · P2 · 交付状态没有统一表达用户拿到什么

- [ ] **把成果入口和授权范围纳入交付摘要。** [源码] `done` 表达本地任务与证据完成，Release 表达集合状态；没有统一字段区分“本地可用、已提交、已推送、预览可访问、已部署”。独立生产环境也不是隔离测试环境的同义词。

**影响：** AI 可能说完成了，用户却不知道怎么打开、改在哪个仓库、服务是否仍在运行；下一轮又需问一遍。

**处理：** 从任务目标派生交付清单：代码位置、被测版本、启动方式或可用地址、Git 目标与同步状态、未完成项。用户只要求本地实现就交付本地结果；已授权发布就自动完成对应发布验证。发布凭据缺失只阻塞那一项，不虚构地址。

**验收：** 用户得到与目标一致的成果入口，不需要询问启动命令或猜测 push/deploy 是否完成。模板外部 provider 的真实能力与本地替身结果明确区分。

### E17 · P1 · 已有数据的升级安全缺少执行证明

- [ ] **增加从旧版本带数据升级到新版本的验证。** [源码＋复现] `scripts/test-migrations.mjs` 只有 `verifyFreshInstall()`：新建空库、应用当前全部 migration、比较结构、重复 deploy。它没有从上一版本部署后写入旧数据，再应用新增 migration 的过程。`validateDesignArtifacts()` 也主要比较最终结构。

**反例：** 隔离 SQLite 样例中，新增 migration 执行 DROP TABLE 后按原结构重建；旧记录从 1 条变成 0 条，真实调用 `validateDesignArtifacts()` 仍返回空错误列表。本次没有对这条合成 migration 跑完整 Prisma 流程；它直接证明结构一致不能证明旧数据保留。

**另一个缺口：** 规范禁止改写已发布 migration，但现有上述验证主要从当前文件重放，没有按已发布基线校验旧 migration 路径与内容是否变化。重写旧文件后能从空库装成功，并不代表已经应用旧版本的数据库能继续升级。

**处理：** 迁移验证分为新建、升级与恢复三个适用场景。由 Plan/发布基线确定旧 migration 集合，校验已应用文件不可变；旧版本写入代表性数据后，应用新增迁移，检查主键、外键、权限归属和业务数据不变量。恢复方式按变更决定：可回退代码、前向修复或备份恢复，不把 `git revert` 等同数据库恢复。

**验收：** 未获目标授权的数据丢失必然使升级验收失败；允许的数据删除必须与稳定契约一致。已应用 migration 被修改或删除时提前失败，并能定位文件。

### E18 · P2 · 执行规则和 SQLite 测试实现耦合较深

- [ ] **把具体环境的实现集中在小型适配层。** [源码] 当前生产约束不允许使用本地 `file:` SQLite；但迁移脚本、E2E、`makeSafeTestEnvironment()` 都使用 `file:` URL，Design 校验通过 `node:sqlite` 执行 SQL。更换 provider 因而不只是修改业务数据库，还要修改多处执行保障代码。

**影响：** 用户未来明确提出更换基础设施时，AI 可能不断修复测试框架的隐含假设，或者放宽验证来换取通过。问题是替换成本和遗漏风险，不是当前 SQLite 本地方案不可用。

**处理：** 保留现有技术栈，将“准备隔离数据库、升级、重置测试数据、比较结构、清理”集中成明确接口；第一次真实引入另一 provider 时实现对应适配。检查策略依赖这些能力，不依赖硬编码文件数据库。运行时能力不支持时明确报告，不生成绿色替代结果。

**验收：** 对明确选定的新 provider，只替换相应适配与配置就能运行同一类业务验收；不会意外写真实生产数据。当前不预先实现所有数据库、移动应用或桌面应用适配器。

### E19 · P1 · 环境指纹没有覆盖全部影响结果的配置

- [ ] **以声明的验证输入构造环境与指纹。** [源码＋复现] `makeSafeTestEnvironment()` 继承全部 `process.env` 再覆盖少数变量；`environmentIdentity()` 只选取 Node/pnpm/平台和少量应用变量。修改实际被 Playwright 配置读取的 `PLAYWRIGHT_EXECUTABLE_PATH` 后，环境指纹不变。新增公开构建变量也没有自动纳入机制。

**影响：** 更换浏览器或新增影响构建、测试行为的配置后，已有成功 run 可能被复用。扩展外部服务时，继承的环境变量是否会导致真实副作用，也没有能力级的统一说明。这里是后续扩展边界，不声称当前项目已经调用真实外部服务。

**处理：** 每个检查步骤声明环境输入、运行工具版本、数据准备标识和副作用边界；必要配置形成不暴露值的指纹。新 adapter 引入变量时同步登记。环境组装采用明确的允许范围，同时保留 PATH 等运行必需项；不得为了隔离任意清空运行环境而破坏工具启动。

**验收：** 切换实际浏览器、影响产物的构建变量或 provider 配置时，相应结果失效；与测试无关的终端变量变化不触发全量重跑；日志与 manifest 不泄露密钥。

依据：`scripts/ignite/core.mjs:269`、`makeSafeTestEnvironment()`、`playwright.config.ts`；本轮环境指纹探针。

### E20 · P1 · 多任务依赖、修改归属和集成基线没有完整协调

- [ ] **统一依赖准入。** [复现] 两个 draft 互相 depends_on，全局 Plan 校验仍为空错误；随后其中一个通过 ready、active 状态转换，下一次全局校验才报另一个依赖未完成。原因是依赖校验主要在 `validateAllPlans()`，状态切换只调用单 Plan 校验，且没有完整的依赖环检测。

**处理：** 对任务依赖做成环、缺失与可执行性检查；状态切换复用相同准入逻辑。依赖循环应给出具体链路，AI 自动调整合理的任务拆分；涉及目标变化才反馈用户。

**验收：** 不会先宣布 active 再让检查拒绝；A → B → A 在准备阶段被明确识别；无依赖或依赖已满足的任务正常执行。

- [ ] **区分任务自己的修改与集成环境的变化。** [复现] A 只修改 `src/a.txt`，范围合法；另一个独立 B 提交 `src/b.txt` 后，A 的 `base_commit...HEAD` 差异包含两个文件，B 被判为 A 越界。全仓输入指纹也会随 B 改变。

**处理：** 最小方案是独立实现使用隔离分支/工作区，并明确单一集成负责人及顺序。任务归属可记录其实现提交或变更集合，集成验证针对合并后的整体快照；保留全仓指纹作为整体回归依据，而不是让 AI 随意扩大 write_scope 消除错误。共享 Schema、API 注册、导航和锁文件的冲突必须有归属与合并检查。

**验收：** 两个互不相干的任务不会被误归为彼此的实现；整合产生的冲突和行为变化有新的集成验证。已知相互影响的任务不会并行写同一工作区。并行是适用时的加速手段，不是每次任务必须使用。

**编号补充：** [源码＋待演练] 脚手架以本地最大 IGT 编号加一，不同工作区可能得到同号。先用单一任务登记点分配编号，或设计可检测、可迁移的唯一标识；不要等合并时才要求用户修复全部引用。

依据：`validateAllPlans()`、`setPlanStatus()`、`changedFilesForPlan()`、`computeInputFingerprint()`；本轮三个独立 Git fixture 演练中的依赖与范围场景。

### E21 · P1 · 合并方式可能破坏历史证据的可达性

- [ ] **将 Git 合并策略纳入证据生命周期。** [复现] feature 分支中的 verifying Plan 指向真实 integrated_commit，验证成功；执行本地 `git merge --squash` 并提交后，代码和 Plan 都存在，校验却拒绝：`integrated_commit must be an ancestor of HEAD`。普通 rebase 也会重写提交身份，但本轮实跑的是 squash 场景。

**影响：** 分支工作本身没有错误，正常合并后却使发布校验变红。新克隆还可能根本取不到被改写的旧提交。E13 的首推处理不能解决这个日常合并问题。

**处理：** 先选定与证据模型兼容的简单策略。最小方案是保留提交身份的合并，并在项目采用和交付预检中检查一致性。若项目选择 squash/rebase，则为最终集成快照重新生成结果，显式关联源提交与集成提交；不能只替换 manifest 里的 hash。这样也能验证合并本身引入的变化。

**验收：** 允许的合并方式在全新克隆中仍可验证；不允许的方式在合并前有明确动作建议。采用 squash/rebase 时有相应再验证流程，用户不需要手工修改 Git 哈希或证据文件。

## 四、工程学角度的逐项判断

| 原则             | 当前判断                         | 需要推进的结果                                      |
| ---------------- | -------------------------------- | --------------------------------------------------- |
| 职责分离、低耦合 | 业务架构较好，AI 容易定位修改层  | 可改外壳与稳定行为约束也应分开，E04                 |
| 契约与配置管理   | 有真实基线、版本与输入指纹       | 关键正文约束和项目身份不能游离于契约之外，E03/E05   |
| 需求追踪         | REQ → AC → Test 已建立           | 补目标到 REQ 的漏项检查及断言语义，E05/E08          |
| 反馈控制         | 实现、测试、修复闭环存在         | 加入失败分类和无进展处理，E11                       |
| 幂等与可恢复性   | 同输入复用、锁和历史证据较好     | 覆盖进程树、中断与坏记录的真实故障，E11             |
| 可观测性         | 有记录、时间、状态、日志         | 让状态直接指导动作，E10                             |
| 防错与可信边界   | 能拒绝多类结构错配               | 证明执行来源，避免自写 JSON 自证，E09               |
| 变更隔离         | 单工作区检查互斥有效             | 不把检查锁等同代码写锁；区分任务归属和集成快照，E20 |
| 版本与兼容管理   | 证据已绑定提交、迁移可从空库重放 | 补带数据升级及合并后证据生命周期，E17/E21           |
| 可替换与可复现   | 本地运行环境和隔离库已规范化     | 验证适配集中、影响结果的环境输入完整，E18/E19       |
| 最小充分验证     | 当前偏向全面保守                 | 风险分层与结果复用要降低重复成本，E06/E12           |
| 人的注意力管理   | 文档能保存历史，但接续仍费力     | 默认给当前任务摘要、下一步和真正阻塞，E10/E16       |
| 持续改进         | 有测试和回归记录                 | 用采用成功率、人工干预次数和恢复时间评价系统        |

本系统具有规范工程的基本形态。主要矛盾是局部机制已经较严密，但端到端体验仍缺少接管、接续和交付的统一编排；同时部分保证超过了实际可信边界。继续增加规范而不修这些接缝，会同时增加 AI 和人的管理负担。

第二轮的补充判断：任务分解需要依赖图与修改归属支持；版本管理需要与实际合并策略匹配；变更验证要覆盖有数据的升级；可扩展性要覆盖验证基础设施本身。这四项决定该工程能否从“模板当前版本能用”继续成长为“反复开发仍然好用”。

## 五、建议处理顺序与最小交付结果

以下是后续实施建议，不是本次已完成事项。沿用当前文档和 CLI 扩展，不先引入新的管理平台。

1. **先保证完成的含义可靠：E05、E08、E09。** 固定目标约束、验收对象与真实执行来源。结果是“通过”有明确、可追溯的含义。
2. **让首次接管与集成交付可靠：E01、E02、E03、E04、E13、E19、E21。** 自动预检、项目归属、资产替换、环境输入和合并协议形成一条路径。结果是用户不用逐个配置文件指导，也不用合并后修证据。
3. **让中途不需要人盯：E10、E11、E20。** 生成下一步与接续摘要，补失败诊断、进程恢复和任务依赖准入。结果是换会话、卡命令、并行任务都能得到明确处理。
4. **让后续修改既轻又能安全升级：E06、E07、E12、E14、E15、E16、E17、E18。** 小任务有小流程，独立任务可以交付，已有数据得到验证，验证实现也方便替换。

每个实施 Plan 只负责一个可独立验收的结果；不要把以上所有问题再次合并为长期大 Plan，也不要为同一个结果的测试、格式、状态回写另建 Plan。

## 六、用结果检验“接近 9.5 分”

建议把以下场景作为下一版的端到端验收矩阵。当前均为待执行的新版验收，不冒充已有 67 项测试的覆盖范围。

- [ ] 冷启动：无 node_modules、无浏览器缓存，AI 从入口完成安装、启动和验证。
- [ ] 衍生采用：完整 clone 与新 Git 历史两种方式都能设定项目身份，保留正确远端归属。
- [ ] 外壳替换：按明确目标更换品牌、主题、首页与主路由，入口和测试同步成立。
- [ ] 示例删除：实际移除 Tasks 整条切片，认证、会话与权限验证仍通过。
- [ ] 普通增量：完成一个新业务切片，目标、实现、测试、事实回写和交付入口相符。
- [ ] 存量修改：修改既有字段或行为，兼容性和迁移影响被识别，不生成无关新模块。
- [ ] 轻量改动：改文案不触发无意义的业务测试编造，也不会漏掉执行规则变化。
- [ ] 草稿隔离：记录未来需求不影响当前任务交付；未来任务自身仍不能带缺项实施。
- [ ] 目标漂移：修改关键约束或删除目标条目时，旧验收不能被静默继续使用。
- [ ] 虚假通过：空断言、错误选择器、伪造 receipt 和放宽测试都触发相应拒绝或审查。
- [ ] 故障恢复：挂起、正常静默、进程中断、坏记录分别恢复，无重复执行或误杀。
- [ ] 上下文恢复：新会话接手不需要用户重述目标，不重复开启已完成步骤。
- [ ] 多任务交付：同 Release 的独立 Plan 可按明确规则集成；整体发布仍完整验收。
- [ ] 首推与 CI：初始、普通 push、PR、浅历史有正确的差异与证据处理。
- [ ] 交付一致：声明可访问的入口确实可用；只完成本地验证时不会宣称已上线。
- [ ] 带数据升级：从旧版本带代表性数据升级，保留目标要求的数据与权限不变量；旧 migration 不被改写。
- [ ] 环境失效：更换实际浏览器或影响产物的配置后，不复用旧验证结果。
- [ ] 依赖调度：依赖环和缺失任务在实施前被定位，状态切换与实际可执行性一致。
- [ ] 合并交付：按声明支持的 merge/squash/rebase 策略，从分支到主干及新克隆均能正确验证。
- [ ] 验证适配：真正替换数据库 provider 时，隔离环境和保障层同步适配，不通过删除测试完成替换。

建议在固定环境、明确目标样本下做至少 20 次跨任务运行，记录首次成功率、人工工程干预次数、无效重复检查时间、失败到恢复耗时、上下文重读成本与漏验数。人工干预不包括用户首次提供目标、必要凭据及无法代替的授权；包括用户催补测试、修路径、解释状态、纠正假完成等工作。

可先把“至少 19/20 次无需额外工程干预、已知错误完成反例全部被发现、相同输入不重复验证”作为内部改进目标。20 次只是早期样本，不足以证明任意项目达到 95% 可靠性；更不能把它直接换算成客观 9.5 分。

## 七、本次复现实验摘要

探针位于本机忽略目录 `.ignite/audit-execution-probes.mjs`，使用临时独立 Git fixture，结束后删除该 fixture。合成证据未写入正式证据目录；主项目源码、测试和既有 Plan 未修改。复现命令：在规范化 WSL/Node 环境中执行 `node .ignite/audit-execution-probes.mjs`。

| 实验                                       | 观察到的结果                  | 对应问题 |
| ------------------------------------------ | ----------------------------- | -------- |
| 在 Plan 正文添加不得删除数据的非目标约束   | 输入指纹未改变                | E05      |
| 纯文字根目录报告与 README 比较最低检查层级 | 分别为 integration、dev       | E06      |
| 指定用例名，提供同 AC 的其他 passing 结果  | 无验收错误                    | E08      |
| 构造两份合规 JSON，不执行其中命令          | Plan 成功进入 done            | E09      |
| 使用全零 DIFF_BASE 调用 CI 完成检查        | 拒绝：不是实际 commit         | E13      |
| 创建引用未定义需求/测试的 future draft     | 全局 Plan 校验报告错误        | E07      |
| adopted 项目使用单引号默认 secret          | doctor 退出码 0               | E03      |
| siteConfig 改为合法双引号写法              | doctor 退出码 1，无法读取身份 | E03      |

以上实验说明当前边界，不说明所有真实任务都会失败。后续修复应把对应反例纳入正式契约测试，并以真实用户旅程验证效果。

报告是新增审计产物，会改变仓库输入指纹；既有完成记录仍按历史被测提交解释。本次不为审计结论编造发布证据，不把已记录问题标为已修复。

### 第二轮复现补充

第二轮使用两个独立审阅方向交叉检查；主审阅者重新阅读相关实现，并复跑了以下迁移、环境和 Git 生命周期探针。探针仍只在临时 fixture 中产生合成状态，没有修改主项目实现或正式证据。

| 实验                                 | 观察结果                                      | 对应问题 |
| ------------------------------------ | --------------------------------------------- | -------- |
| 旧库放入 1 条数据，DROP 后同结构重建 | 数据为 0，Design 结构校验仍无错               | E17      |
| 仅改变浏览器路径和新增公开构建变量   | 环境指纹未改变                                | E19      |
| 两个 draft 形成循环依赖              | 全局校验无错，active 转换成功后全局校验才失败 | E20      |
| A 后面集成不相关 B 的提交            | A 的差异包含 B，并报 B 越界                   | E20      |
| 原分支 verifying Plan 经 squash 合入 | 原校验成功，合入后祖先关系校验失败            | E21      |

迁移与环境探针位于本机 `.ignite/audit-architecture-probes.mjs`；Git 生命周期探针位于本机临时目录 `ignite-lifecycle-review.mjs`。这些不是正式发布测试；本轮没有远端 push、GitHub 合并或生产数据迁移。

## 八、建议的最小执行架构

缺陷清单还需要有一个共同落点，否则修完 20 多个问题可能产生 20 多套小流程。建议保留当前模块化单体、文档目录和 `pnpm ignite` 入口，在现有实现内明确以下职责；表格表达职责，不要求新建同等数量的目录或服务。

| 职责           | 负责什么                                      | 现有位置与最小演进                     | 输出给下一环节                         |
| -------------- | --------------------------------------------- | -------------------------------------- | -------------------------------------- |
| 执行契约       | 承接已确定目标、范围、约束、验收和授权        | Feature＋Plan 的稳定字段；补 E05       | 本轮允许做什么、必须证明什么           |
| 状态与动作判定 | 依赖是否满足、当前该实施/等待/验证/诊断/交付  | state/cli 的统一判定函数               | 有原因的下一步、可执行条件、真正阻塞   |
| 工作执行       | AI 修改代码，工具执行命令，适配器准备隔离环境 | 宿主 AI＋现有 scripts/runs；补 E18/E20 | 变更集合、提交、运行记录、可恢复检查点 |
| 独立验证       | 对比目标、实际发现用例、执行结果和输入        | checks/reporters/CI；补 E08/E09/E19    | 哪些要求已证明、哪些未证明、结果来源   |
| 集成与交付     | 合并后的验证、事实回写、成果入口、下一轮接续  | Release/Design/status；补 E10/E16/E21  | 用户可取得的结果及准确的后续状态       |

关键是形成一个统一的动作判定接口：输入当前 Plan、Git 状态、运行结果和环境能力，输出当前允许的下一步、原因、相关文件和阻塞。`status`、`set-status`、`check` 与 CI 共用同一套判定；读取状态时不擅自执行动作。宿主 AI 消费这些结果来写代码、调用工具和继续流程。

例如，有相同输入的活动 run，就返回等待该 run；目标测试失败，就返回失败类别和相关验收项；实现已提交而缺 release 证据，就给出准确的验证步骤；证据齐备但要求的成果入口尚未就绪，就继续交付步骤。这样下一步来自事实，不要求用户告诉 AI “现在去测试”“继续跑完”“把文档补上”。

不应把一个新的 `next_action` 手写文件变成第二份事实源。Plan 维护契约与过程检查点，run 维护执行事实，下一步和摘要按需派生。现有 draft/ready/active/verifying/done 状态继续是任务状态，检查步骤只是任务内的进度，避免再建一套相互冲突的状态机。

### 文档是否已经说完整，以及应该补在哪里

**目前没有完全说完整。** 现有文档已说明目录职责、主业务链路和规格驱动 Loop；部分要求已经写出，但执行器未完整落实，例如旧 migration 不可改写、Plan 独立交付。还有部分跨环节规则没有明确约定，例如任务依赖准入、合并策略与证据如何衔接。应分别处理“缺说明”和“缺实现”，不能用补一段规范代替修复执行器。

| 现有真源                                   | 后续应补的内容                                                       |
| ------------------------------------------ | -------------------------------------------------------------------- |
| `AGENTS.md`                                | 保持短入口，说明接管、接续与交付使用哪个统一入口；不复制具体长规则   |
| `docs/standards/workflow.md`               | 统一状态准入、依赖、失败恢复、轻量任务、集成和交付协议               |
| `docs/standards/adoption.md`               | 项目身份、Git 归属、可替换资产、冷启动与支持的环境能力               |
| Feature / Plan 模板                        | 稳定目标与约束、授权、交付对象、验收映射和必要检查点                 |
| `docs/standards/testing.md`、`database.md` | 测试发现与实际结果对账、可信边界、带数据升级与恢复要求               |
| `docs/designs/runtime.md`                  | 只描述当时已实现的 CLI、状态、证据和适配行为；不要提前把建议写成事实 |

本报告只是待办索引，不成为新的长期规则真源。后续每项按对应 Plan 落地，同步上述位置与实际测试后，才能将本报告中的待办勾选完成。

### 日常使用时，人只应承担什么

用户提供已经确定的目标和必要的现实输入；执行系统保存它们并在后续继续使用。范围内、可回滚的常规实现、测试、格式修复、文档更新和接续由 AI 自行完成。确实缺少凭据、目标出现影响结果的歧义或需要额外授权时，系统汇总缺项与可继续的工作，避免逐个技术问题打断用户。

减少人的注意力消耗，应通过“已知信息不重问、可自动验证的不转交、失败有恢复动作、完成有成果入口”衡量。不能通过隐藏失败、删除验证或仅减少消息数量来获得表面上的顺畅。

### 控制改造复杂度

- [ ] 先统一现有函数的判定与结果结构，再增加方便 AI 调用的入口。
- [ ] 同一个信息只维护一份，其余是派生视图；不新增平行规范目录或第二份状态台账。
- [ ] 规则与执行适配分开，但只实现当前需要的环境与能力。
- [ ] 普通模块修改不用顺带重写执行框架；执行框架升级有自己的验收和兼容验证。
- [ ] 每个改进用一个实际采用/修改/中断/合并场景证明节省了多少人工干预。
