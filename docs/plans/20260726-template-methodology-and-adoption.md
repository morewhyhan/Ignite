# Ignite 实施计划：模板方法论与采用体验完善

## 状态

已完成。

## 变更类型

- 类型：`[存量改动]`
- 影响的存量路径：`AGENTS.md`、`README.md`、`docs/`、验证脚本、E2E 配置和少量契约实现
- 新增的增量路径：模板采用规范、规格驱动工作流、测试用例模板和文档检查
- 兼容性影响：不改变现有页面入口；输入契约和验证命令变化会明确记录
- 数据迁移或回滚要求：不修改现有 migration，不删除用户数据

## 目标

- 把手册中的需求规格、Plan/Go、TDD Core Loop、E2E Loop 和 Agent as Code 方法转化为可执行规则。
- 以首次采用者视角消除启动、验证、替换示例业务和新增模块时的主要摩擦。
- 让文档、API 契约、数据库快照和实际实现之间的漂移可以被检查。

## 输入规格

- Feature：`docs/features/product.md`
- Standards：`docs/standards/`
- Designs：`docs/designs/`
- Source of truth：`src/`、`prisma/`、`tests/`、`package.json`
- 外部参考：团队级 AI Coding 简明手册 v0.2 的文档组织与研发闭环方法

## 已关闭问题

- 工具选择不纳入本轮；只采用方法和理论。
- `tasks` 保留为可删除的参考纵向切片，不自动删除。
- 测试用例归入 `docs/others/test-cases/`，遵循手册的五类主目录定义。
- 用户本轮已明确要求实施，因此不需要额外重复确认 Plan。

## 实现任务

- [x] 建立模板资产分级与首次采用手册。
- [x] 完善 Feature、Plan 和测试用例模板。
- [x] 把 TDD/API/E2E Loop 与准出条件写入 AI 规则。
- [x] 修复本地构建验证和 E2E 数据隔离问题。
- [x] 修正文档与 API、数据库实现之间的已知漂移。
- [x] 增加文档与计划自动检查并接入本地验证和 CI。
- [x] 补齐认证与示例业务解耦的测试和说明。
- [x] 运行完整验证并记录结果。

## 验收方式

| 验收目标                             | 验证方式                     | 结果                                  |
| ------------------------------------ | ---------------------------- | ------------------------------------- |
| 文档体系可被机器检查                 | `pnpm docs:check`            | 36 Markdown / 3 Feature / 2 Plan 通过 |
| 类型、边界、格式和行为保持正确       | `pnpm check`                 | 通过，28 个 Vitest 测试               |
| migration 基线仍可重复部署且无 drift | `pnpm test:migrations`       | 通过                                  |
| 本地生产构建可以在安全测试配置下完成 | `pnpm build` / `pnpm verify` | 通过                                  |
| E2E 使用独立端口和临时数据库         | `pnpm test:e2e`              | 通过，3 个 Playwright 测试            |
| 工作区无空白错误                     | `git diff --check`           | 通过                                  |

## 状态记录

| 时间       | 状态   | 说明                                              |
| ---------- | ------ | ------------------------------------------------- |
| 2026-07-26 | 进行中 | 完成 PDF 方法核对和三路只读审计，开始落地改进     |
| 2026-07-26 | Red    | 新增环境与严格输入契约测试，确认当前实现 6 项失败 |
| 2026-07-26 | Green  | 修复环境语义和严格 Zod 输入后，24 个目标测试通过  |
| 2026-07-26 | 已完成 | 完整 verify、migration、build 和隔离 E2E 全部通过 |

## 准出条件

- [x] 所有适用检查通过。
- [x] 失败或未执行项有明确原因。
- [x] `docs/designs/` 已同步最终事实。
- [x] README 和 AGENTS 能将首次采用者路由到唯一采用手册。
